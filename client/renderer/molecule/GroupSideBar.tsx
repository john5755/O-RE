import styled from "@emotion/styled";
import React from "react";

const Container = styled.div`
  width: 100%;
  height: 100%;
  background-color: var(--main-color);
`;

export default function GroupSideBar() {
  return <Container>GroupSideBar</Container>;
}
